import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  footerContainer: {
    height: "5rem",
    backgroundColor: "#333",
    margin: "0 auto",
  },
  footerText: {
    color: "white",
    fontSize: "1.35rem",
  },
});
